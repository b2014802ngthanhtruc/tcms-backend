export enum ScopeClass {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

export enum PaymentFrequency {
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  NEGOTIAL = 'negotial',
}

export enum TeachingMode {
  ONLINE = 'online',
  OFFLINE = 'offline',
}

export enum ClassStatus {
  NEW = 'new',
  PENDING = 'pending',
  STARTED = 'started',
  PAUSED = 'paused',
  CANCELED = 'canceled',
  ENDED = 'ended',
}
